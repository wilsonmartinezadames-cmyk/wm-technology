import axios from 'axios'\n\nconst API_BASE_URL = import.meta.env.VITE_API_URL || 'http://localhost:5000/api'\n\nconst api = axios.create({\n  baseURL: API_BASE_URL,\n  timeout: 10000,\n})\n\n// Interceptor para agregar JWT token\napi.interceptors.request.use(\n  (config) => {\n    const token = localStorage.getItem('authToken')\n    if (token) {\n      config.headers.Authorization = `Bearer ${token}`\n    }\n    return config\n  },\n  (error) => Promise.reject(error)\n)\n\n// Interceptor para manejar errores\napi.interceptors.response.use(\n  (response) => response,\n  (error) => {\n    if (error.response?.status === 401) {\n      localStorage.removeItem('authToken')\n      localStorage.removeItem('user')\n      window.location.href = '/login'\n    }\n    return Promise.reject(error)\n  }\n)\n\n// Auth endpoints\nexport const authAPI = {\n  register: (email, password, name) =>\n    api.post('/auth/register', { email, password, name }),\n  login: (email, password) =>\n    api.post('/auth/login', { email, password }),\n  logout: () => {\n    localStorage.removeItem('authToken')\n    localStorage.removeItem('user')\n  },\n  refreshToken: () => api.post('/auth/refresh-token'),\n  resetPassword: (email) => api.post('/auth/reset-password', { email }),\n  verifyResetCode: (email, code, password) =>\n    api.post('/auth/verify-reset', { email, code, password }),\n  getProfile: () => api.get('/auth/profile'),\n}\n\n// User endpoints\nexport const userAPI = {\n  getProfile: () => api.get('/users/profile'),\n  updateProfile: (data) => api.put('/users/profile', data),\n  uploadAvatar: (formData) =>\n    api.post('/users/avatar', formData, {\n      headers: { 'Content-Type': 'multipart/form-data' },\n    }),\n  changePassword: (oldPassword, newPassword) =>\n    api.post('/users/change-password', { oldPassword, newPassword }),\n  getSubscription: () => api.get('/users/subscription'),\n}\n\n// Payment endpoints\nexport const paymentAPI = {\n  createCheckout: (plan) => api.post('/payments/checkout', { plan }),\n  getInvoices: () => api.get('/payments/invoices'),\n  getSubscription: () => api.get('/payments/subscription'),\n  updateSubscription: (plan) => api.put('/payments/subscription', { plan }),\n  recordPayment: (amount, method, description) =>\n    api.post('/payments/record', { amount, method, description }),\n}\n\n// Contact endpoint\nexport const contactAPI = {\n  sendMessage: (name, email, message, subject) =>\n    api.post('/contact', { name, email, message, subject }),\n}\n\nexport default api\n