import { motion } from 'framer-motion'\nimport { useState } from 'react'\nimport { Link } from 'react-router-dom'\nimport { useAuth } from '../context/AuthContext'\n\nconst PasswordRecovery = () => {\n  const [step, setStep] = useState(1) // 1: email, 2: code, 3: reset\n  const [formData, setFormData] = useState({\n    email: '',\n    code: '',\n    password: '',\n    confirmPassword: '',\n  })\n  const [loading, setLoading] = useState(false)\n  const [error, setError] = useState('')\n  const [success, setSuccess] = useState('')\n  const { resetPassword } = useAuth()\n\n  const handleChange = (e) => {\n    setFormData({\n      ...formData,\n      [e.target.name]: e.target.value,\n    })\n  }\n\n  const handleStep1 = async (e) => {\n    e.preventDefault()\n    setError('')\n    setLoading(true)\n\n    try {\n      await resetPassword(formData.email)\n      setSuccess('Código enviado a tu email')\n      setStep(2)\n    } catch (err) {\n      setError(err.message)\n    } finally {\n      setLoading(false)\n    }\n  }\n\n  const handleStep2 = (e) => {\n    e.preventDefault()\n    if (formData.code.length < 4) {\n      setError('Código debe tener al menos 4 caracteres')\n      return\n    }\n    setStep(3)\n    setError('')\n  }\n\n  const handleStep3 = async (e) => {\n    e.preventDefault()\n    setError('')\n\n    if (formData.password !== formData.confirmPassword) {\n      setError('Las contraseñas no coinciden')\n      return\n    }\n\n    if (formData.password.length < 8) {\n      setError('La contraseña debe tener al menos 8 caracteres')\n      return\n    }\n\n    setLoading(true)\n\n    try {\n      // Aquí irá la lógica de resetear contraseña\n      setSuccess('Contraseña actualizada correctamente')\n      setTimeout(() => {\n        window.location.href = '/login'\n      }, 2000)\n    } catch (err) {\n      setError(err.message)\n    } finally {\n      setLoading(false)\n    }\n  }\n\n  return (\n    <div className=\"min-h-screen flex items-center justify-center py-20\">\n      <motion.div\n        initial={{ opacity: 0, y: 20 }}\n        animate={{ opacity: 1, y: 0 }}\n        className=\"w-full max-w-md glass p-12 rounded-2xl space-y-8\"\n      >\n        {/* Header */}\n        <div className=\"text-center space-y-2\">\n          <div className=\"text-4xl mb-4\">🔑</div>\n          <h1 className=\"text-3xl font-bold bg-gradient-neon bg-clip-text text-transparent\">\n            Recuperar Contraseña\n          </h1>\n          <p className=\"text-gray-400 text-sm\">Paso {step} de 3</p>\n        </div>\n\n        {/* Progress Bar */}\n        <div className=\"flex gap-2\">\n          {[1, 2, 3].map((s) => (\n            <div\n              key={s}\n              className={`h-1 flex-1 rounded-full transition-all ${\n                s <= step ? 'bg-gradient-neon' : 'bg-white/10'\n              }`}\n            />\n          ))}\n        </div>\n\n        {/* Messages */}\n        {error && (\n          <motion.div\n            initial={{ opacity: 0 }}\n            animate={{ opacity: 1 }}\n            className=\"bg-red-500/20 border border-red-500/50 rounded-lg p-4 text-red-400 text-sm\"\n          >\n            {error}\n          </motion.div>\n        )}\n\n        {success && (\n          <motion.div\n            initial={{ opacity: 0 }}\n            animate={{ opacity: 1 }}\n            className=\"bg-green-500/20 border border-green-500/50 rounded-lg p-4 text-green-400 text-sm\"\n          >\n            ✓ {success}\n          </motion.div>\n        )}\n\n        {/* Step 1: Email */}\n        {step === 1 && (\n          <form onSubmit={handleStep1} className=\"space-y-6\">\n            <div>\n              <label className=\"block text-sm font-semibold mb-2\">Email</label>\n              <input\n                type=\"email\"\n                name=\"email\"\n                value={formData.email}\n                onChange={handleChange}\n                required\n                className=\"w-full bg-white/5 border border-white/20 rounded-lg px-4 py-3 focus:border-neon-cyan focus:outline-none transition-all\"\n                placeholder=\"tu@email.com\"\n              />\n            </div>\n            <motion.button\n              whileHover={{ scale: 1.05 }}\n              whileTap={{ scale: 0.95 }}\n              type=\"submit\"\n              disabled={loading}\n              className=\"w-full bg-gradient-neon text-black font-bold py-3 rounded-lg transition-all disabled:opacity-50\"\n            >\n              {loading ? 'Enviando...' : 'Enviar Código'}\n            </motion.button>\n          </form>\n        )}\n\n        {/* Step 2: Code */}\n        {step === 2 && (\n          <form onSubmit={handleStep2} className=\"space-y-6\">\n            <div>\n              <label className=\"block text-sm font-semibold mb-2\">\n                Código de Verificación\n              </label>\n              <input\n                type=\"text\"\n                name=\"code\"\n                value={formData.code}\n                onChange={handleChange}\n                required\n                className=\"w-full bg-white/5 border border-white/20 rounded-lg px-4 py-3 focus:border-neon-cyan focus:outline-none transition-all text-center text-2xl letter-spacing-wide\"\n                placeholder=\"1234\"\n                maxLength=\"6\"\n              />\n              <p className=\"text-gray-400 text-sm mt-2\">\n                Revisa tu email. Enviamos un código de 4-6 dígitos.\n              </p>\n            </div>\n            <motion.button\n              whileHover={{ scale: 1.05 }}\n              whileTap={{ scale: 0.95 }}\n              type=\"submit\"\n              className=\"w-full bg-gradient-neon text-black font-bold py-3 rounded-lg transition-all\"\n            >\n              Verificar Código\n            </motion.button>\n          </form>\n        )}\n\n        {/* Step 3: New Password */}\n        {step === 3 && (\n          <form onSubmit={handleStep3} className=\"space-y-5\">\n            <div>\n              <label className=\"block text-sm font-semibold mb-2\">\n                Nueva Contraseña\n              </label>\n              <input\n                type=\"password\"\n                name=\"password\"\n                value={formData.password}\n                onChange={handleChange}\n                required\n                className=\"w-full bg-white/5 border border-white/20 rounded-lg px-4 py-3 focus:border-neon-cyan focus:outline-none transition-all\"\n                placeholder=\"Mínimo 8 caracteres\"\n              />\n            </div>\n            <div>\n              <label className=\"block text-sm font-semibold mb-2\">\n                Confirmar Contraseña\n              </label>\n              <input\n                type=\"password\"\n                name=\"confirmPassword\"\n                value={formData.confirmPassword}\n                onChange={handleChange}\n                required\n                className=\"w-full bg-white/5 border border-white/20 rounded-lg px-4 py-3 focus:border-neon-cyan focus:outline-none transition-all\"\n                placeholder=\"Repite tu contraseña\"\n              />\n            </div>\n            <motion.button\n              whileHover={{ scale: 1.05 }}\n              whileTap={{ scale: 0.95 }}\n              type=\"submit\"\n              disabled={loading}\n              className=\"w-full bg-gradient-neon text-black font-bold py-3 rounded-lg transition-all disabled:opacity-50\"\n            >\n              {loading ? 'Actualizando...' : 'Actualizar Contraseña'}\n            </motion.button>\n          </form>\n        )}\n\n        {/* Back to Login */}\n        <div className=\"text-center text-sm text-gray-400\">\n          <Link\n            to=\"/login\"\n            className=\"text-neon-cyan hover:text-neon-purple transition-colors\"\n          >\n            ← Volver a Login\n          </Link>\n        </div>\n      </motion.div>\n    </div>\n  )\n}\n\nexport default PasswordRecovery\n